---
const navLinks = [
    { href: "/", label: "Home" },
    { href: "/#work", label: "Work" },
    { href: "/experiences", label: "Experiences" },
    { href: "/about", label: "About" },
    { href: "/colors", label: "Colors" },
];
---
<div class="
    backdrop-blur-md bg-background/80
    sticky top-0
    mx-auto w-full flex items-center justify-between px-8 py-8"
    >

    <!-- Nav links (center) -->
    <nav class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2 flex gap-x-4 text-xl font-medium text-text mx-1">
        {navLinks.map(link => (
            <a 
                href={link.href} 
                class="btn btn-fill-left py-1.5 px-2.5"
                data-astro-prefetch
            >
                {link.label}
            </a>
        ))}
    </nav>

</div>

